name: Release

on:
  push:
    tags:
      - 'v*' # v1.0.0, v0.2.0-alpha, ...

jobs:
  release:
    name: Publish to NPM
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write #  NPM Trusted Publishers (OIDC)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup
        uses: ./.github/actions/setup
        with:
          registry-url: 'https://registry.npmjs.org'

      - name: Build package
        run: yarn prepare

      - name: Publish to NPM
        run: |
          if [[ "${{ github.ref_name }}" == *"alpha"* ]] || [[ "${{ github.ref_name }}" == *"beta"* ]] || [[ "${{ github.ref_name }}" == *"rc"* ]]; then
            echo "Pre-release detected. Publishing with --tag next"
            npm publish --provenance --access public --tag next
          else
            echo "Stable release detected. Publishing to latest"
            npm publish --provenance --access public
          fi

      - name: Sync master branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin master
          git checkout master
          git merge ${{ github.ref_name }} --ff-only
          git push origin master
